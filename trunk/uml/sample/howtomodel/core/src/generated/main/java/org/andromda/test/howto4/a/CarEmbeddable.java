// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by EntityEmbeddable.vsl in andromda-ejb3-cartridge.
//
package org.andromda.test.howto4.a;

import java.io.Serializable;
import javax.persistence.AttributeOverride;
import javax.persistence.AttributeOverrides;
import javax.persistence.Column;
import javax.persistence.EmbeddedId;
import javax.persistence.JoinColumn;
import javax.persistence.ManyToOne;
import javax.persistence.MappedSuperclass;

/**
 * Autogenerated POJO EJB mapped super class for Car containing the
 * bulk of the entity implementation.
 *
 * This is a mapped super class and autogenerated by AndroMDA using the EJB3
 * cartridge.
 *
 * DO NOT MODIFY this class.
 *
 * 
 */
@MappedSuperclass
public abstract class CarEmbeddable implements Serializable
{
    private static final long serialVersionUID = -6793461983486533447L;

    // ----------- 3 Attribute Definitions ------------
    private CarPK pk;
    private String type;

    // --------- 1 Relationship Definitions -----------
    private Person owner;

    // ---- Manageable Display Attributes (Transient) -----

    // ----- Composite PK Accessors ------
    /**
     * Return the composite primary key for Car
     *
     * @return The composite primary key
     */
    @EmbeddedId
    @AttributeOverrides
    ({
        @AttributeOverride(name = "serial", column = @Column(name = "SERIAL", unique = true, nullable = false)),
        @AttributeOverride(name = "name", column = @Column(name = "NAME", unique = true, nullable = false))
    })
    public CarPK getPk()
    {
        return this.pk;
    }

    /**
     * Set the composite primary key for Car
     *
     * @param pk The composite primary key
     */
    public void setPk(CarPK pk)
    {
        this.pk = pk;
    }

    // -------- 3 Attribute Accessors ----------
    /**
     * Get the type property.
     * 
     * @return String The value of type
     */
    @Column(name = "TYPE", unique = true, nullable = false, insertable = true, updatable = true)
    public String getType()
    {
        return this.type;
    }

    /**
     * Set the type property.
     * @param value the new value
     */
    public void setType(String value)
    {
        this.type = value;
    }


    // ------------- 1 Relations ------------------
    /**
     * Get the owner
     *
     * @return Person
     */
    @ManyToOne(optional = false)
    @JoinColumn(name = "OWNER_FK")
    public Person getOwner()
    {
        return this.owner;
    }

   /**
    * Set the owner
    *
    * @param ownerIn
    */
    public void setOwner(Person ownerIn)
    {
        this.owner = ownerIn;
    }

    // --------------- Constructors -----------------

    /**
     * Default empty constructor
     */
    public CarEmbeddable()
    {
        // Default empty constructor
    }

    /**
     * Implementation for the constructor with all POJO attributes except auto incremented identifiers.
     * This method sets all POJO fields defined in this class to the values provided by
     * the parameters.
     *
     * @param type Value for the type property
     */
    public CarEmbeddable(String type)
    {
        setType(type);
    }

    /**
     * Constructor with all POJO attribute values and CMR relations.
     *
     * @param type Value for the type property
     * @param owner Value for the owner relation
     */
    public CarEmbeddable(String type, Person owner)
    {
        setType(type);

        setOwner(owner);
    }

    /**
     * Constructor with composite primary key arg only
     *
     * @param pk Composite primary key
     */
    public CarEmbeddable(CarPK pk)
    {
        this.pk = pk;
    }

    // -------- Common Methods -----------

    /**
     * Indicates if the argument is of the same type and all values are equal.
     *
     * @param object The target object to compare with
     * @return boolean True if both objects a 'equal'
     */
    public boolean equals(Object object)
    {
        if (this == object)
        {
            return true;
        }
        if (!(object instanceof CarEmbeddable))
        {
            return false;
        }
        final CarEmbeddable that = (CarEmbeddable)object;
        if (this.getPk().getSerial() == null || that.getPk().getSerial() == null || !this.getPk().getSerial().equals(that.getPk().getSerial()))
        {
            return false;
        }
        if (this.getPk().getName() == null || that.getPk().getName() == null || !this.getPk().getName().equals(that.getPk().getName()))
        {
            return false;
        }
        return true;
    }

    /**
     * Returns a hash code value for the object
     *
     * @return int The hash code value
     */
    public int hashCode()
    {
        int hashCode = 0;
        hashCode = 29 * hashCode + (this.getPk().getSerial() == null ? 0 : this.getPk().getSerial().hashCode());
        hashCode = 29 * hashCode + (this.getPk().getName() == null ? 0 : this.getPk().getName().hashCode());

        return hashCode;
    }

    /**
     * Returns a String representation of the object
     *
     * @return String Textual representation of the object displaying name/value pairs for all attributes
     */
    public String toString()
    {
        StringBuilder sb = new StringBuilder();
        sb.append("CarEmbeddable(=");
        sb.append("pk.serial: ");
        sb.append(this.getPk().getSerial());
        sb.append(", pk.name: ");
        sb.append(this.getPk().getName());
        sb.append(", type: ");
        sb.append(getType());
        sb.append(")");
        return sb.toString();
    }

}