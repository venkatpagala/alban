<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<parent>
		<groupId>org.andromda.cartridges</groupId>
    		<artifactId>cartridges</artifactId>
		<version>1.0</version>
	</parent>

	<modelVersion>4.0.0</modelVersion>
	<!--<groupId>org.andromda.cartridges</groupId>-->
        <artifactId>andromda-seam-cartridge</artifactId>
        <version>3.4</version>
	<!-- <packaging>andromda-cartridge</packaging> -->	
	<packaging>jar</packaging>
    <name>AndroMDA Seam Cartridge</name>
    <description>
        The JBoss Seam cartridge is used to generate JSF/Facelet applications from UML models using both static and
        dynamic model elements. Generated applications have default security and internationalization
        implementations and can be deployed as they are.
    </description>
    <!-- TODO it is a copy of andromda-gui-cartridge with a few change in cartridge.xml -->
    
    <dependencies>
        <!--
        <dependency>
            <groupId>org.andromda.cartridges</groupId>
            <artifactId>andromda-jsf-cartridge-components</artifactId>
            <version>${andromda.version}</version>
        </dependency>
        <dependency>
            <groupId>org.andromda.cartridges</groupId>
            <artifactId>andromda-jsf-cartridge</artifactId>
            <version>${andromda.version}</version>
            <scope>compile</scope>
        </dependency>
        -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>andromda-meta-cartridge</artifactId>
            <version>${andromda.version}</version>
            <scope>runtime</scope> 
        </dependency>  
        <dependency>
                <groupId>dom4j</groupId>
                <artifactId>dom4j</artifactId>
        </dependency>        
        <!--
        <dependency>
            <groupId>org.andromda.profiles.uml14</groupId>
            <artifactId>andromda-profile</artifactId>
            <type>xml.zip</type>
        </dependency>
        -->
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.andromda.maven.plugins</groupId>
                <artifactId>andromda-maven-plugin</artifactId>
                <version>${andromda.version}</version>
            </plugin>
            <plugin>
                <groupId>org.andromda.maven.plugins</groupId>
                <artifactId>andromda-cartridge-plugin</artifactId>
                <version>${andromda.version}</version>
                <executions>
                    <execution>
                        <id>seam-uml2</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <!-- Use update goal when you want to validate your change (comment test goal) -->
                            <goal>test</goal>
                            <!--<goal>update</goal>-->
                        </goals>
                        <configuration>
                            <expectedOutputArchive>${project.basedir}/src/test/expected/cartridge-outputUML2.zip</expectedOutputArchive>
                            <configurationUri>file:${project.basedir}/src/main/config/${uml.type}/test/andromda.xml</configurationUri>

                            <actualDirectory>${project.basedir}/../tmp-seam/cartridge-test/actualUML2</actualDirectory>
                            <expectedDirectory>${project.basedir}/../tmp-seam/cartridge-test/expectedUML2</expectedDirectory>                            
                            <reportDirectory>${project.basedir}/../tmp-seam/cartridge-test/reportsUML2</reportDirectory>
                        </configuration>
                    </execution>
                </executions>          
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
		<artifactId>maven-source-plugin</artifactId>
		<!-- <version>${maven.source.plugin.version}</version>-->
		<executions>
                        <execution>
                            <id>attach-sources</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
            </plugin> 
                        <plugin>
                                <groupId>org.codehaus.mojo</groupId>
                                <artifactId>xml-maven-plugin</artifactId>
                                <version>${maven.xml.plugin.version}</version>
                        </plugin>
            		<plugin>
            		    <groupId>org.andromda.maven.plugins.site</groupId>
            		    <artifactId>andromda-site-cartridge-plugin</artifactId>
            		    <version>${andromda.version}</version>
            		    <executions>
            		        <execution>
            		            <id>cartridge-howto-artifacts</id>
            		            <goals>
            		                <goal>generate-cartridge-howto-artifacts</goal>
            		            </goals>
            		        </execution>
            		    </executions>
            		    <!-- http://www.andromda.org/docs/andromda-maven-plugins/andromda-site-plugins/andromda-site-cartridge-plugin/generate-cartridge-howto-artifacts-mojo.html -->
            		    <configuration>
            		    	<configurationUri>file:${project.basedir}/src/main/config/${uml.type}/howto/andromda.xml</configurationUri>            		    	
            		        <howtoCartridgePicturesOutputDirectory>${project.basedir}/${target.dir}/site/images</howtoCartridgePicturesOutputDirectory>
            		        <modelOutputHistory>${project.basedir}/../tmp-seam/history</modelOutputHistory>
            		    </configuration>
            		</plugin>   
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>${maven.clean.plugin.version}</version>
                <configuration>
                   <filesets>
                       <fileset>
                           <directory>${project.basedir}/../tmp-ejb3</directory>
                           <includes>
                               <include>**/*</include>
                           </includes>
                       </fileset>
                       <fileset>
                           <directory>${project.basedir}/null</directory>
                           <includes>
                               <include>**/*</include>
                           </includes>
                       </fileset>
                   </filesets>
                </configuration>
            </plugin>   
        </plugins>
    </build>
    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-changes-plugin</artifactId>
                <version>${maven.changes.plugin.version}</version>
                <configuration>
                    <xmlPath>${basedir}/src/site/changes.xml</xmlPath>
                </configuration>
                <reportSets>
                    <reportSet>
                        <reports>
                            <report>changes-report</report>
                            <report>jira-report</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>          
        </plugins>
    </reporting>
    <properties>
        <!--<model.name>SeamMetafacadeModel</model.name>-->
        <model.name>GuiMetafacadeModel</model.name>
    	<lastModifiedCheck>true</lastModifiedCheck> <!-- for mvn site to work with andromda-cartridge-plugin must be to true -->
        <!--<test.model.uri>file:${test.src.dir}/${uml.type}/SeamCartridgeTestModel.uml</test.model.uri>-->
        <test.model.uri>file:${test.src.dir}/${uml.type}/GuiCartridgeTestModel.uml</test.model.uri>
        <!--<test.output.dir>${project.basedir}/${target.dir}/cartridge-test/actualUML2</test.output.dir>-->
        <test.output.dir>${project.basedir}/../tmp-seam/cartridge-test/actualUML2</test.output.dir>
    </properties>
</project>
